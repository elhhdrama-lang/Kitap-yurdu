<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Satıcı Kaydı - Kitapyurdu</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/css/site.css" rel="stylesheet" />
    <style>
        body { 
            background-color: var(--background-color); 
            display: flex; 
            align-items: center; 
            justify-content: center; 
            min-height: 100vh;
            padding: 2rem 0;
        }
        .register-card { 
            width: 100%; 
            max-width: 600px; 
        }
    </style>
</head>
<body>
    <div class="register-card">
        <div class="auth-card">
            <div class="text-center mb-4">
                <i class="bi bi-shop fs-1 text-primary"></i>
                <h2 class="fw-bold mt-2">Satıcı Başvurusu</h2>
                <p class="text-muted small">E-ticaret dünyasında yerinizi alın.</p>
            </div>

            @if (ViewBag.Error != null)
            {
                <div class="alert alert-danger border-0 shadow-sm rounded-3">@ViewBag.Error</div>
            }

            <form action="/Seller/Register" method="post">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label small fw-bold text-secondary">Kullanıcı Adı</label>
                        <input type="text" name="Username" class="form-control" required />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label small fw-bold text-secondary">Şifre</label>
                        <input type="password" name="PasswordHash" class="form-control" required />
                    </div>
                </div>
                <div class="mb-3 mt-3">
                    <label class="form-label small fw-bold text-secondary">E-posta</label>
                    <input type="email" name="Email" class="form-control" placeholder="ornek@sirket.com" required />
                </div>
                <div class="mb-3">
                    <label class="form-label small fw-bold text-secondary">Şirket / Mağaza Adı</label>
                    <input type="text" name="CompanyName" class="form-control" required />
                </div>
                 <div class="mb-3">
                    <label class="form-label small fw-bold text-secondary">İletişim Bilgileri (Tel/Adres)</label>
                    <textarea name="ContactInfo" class="form-control" rows="2"></textarea>
                </div>
                
                <div class="d-grid gap-2 mt-4">
                    <button type="submit" class="btn btn-primary btn-lg shadow-sm">
                        <i class="bi bi-check-circle me-1"></i> Başvuruyu Tamamla
                    </button>
                    <a href="/Seller/Login" class="btn btn-link btn-sm text-muted">
                        Zaten hesabınız var mı? <span class="text-primary fw-bold">Giriş yapın</span>
                    </a>
                </div>
            </form>
        </div>
        <p class="text-center text-muted small mt-3">
            &copy; 2025 Web Kitapyurdu Satıcı Paneli
        </p>
    </div>
</body>
</html>
